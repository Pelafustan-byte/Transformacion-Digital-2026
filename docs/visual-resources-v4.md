# Visual Resources v4

The CMS now includes purpose-built vector illustrations for library collections and capsule covers, plus scalable Ruta Digital and Contraloría marks. The public portal references these local assets directly, while editors can later replace any image from `/admin/` without touching code.
